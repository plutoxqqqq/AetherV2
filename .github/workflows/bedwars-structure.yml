name: BedWars structure

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'BedWars modules now live in games/6872274481.lua. This workflow is retired.'
