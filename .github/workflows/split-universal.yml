name: Generate Universal Modules

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Universal modules now live in games/universal.lua. This workflow is retired.'
