# AetherV2 BedWars API package

This archive is intentionally rooted at `libraries/bedwars/`.

Extract the ZIP from the AetherV2 repository root and the files will land in the correct location without an extra wrapper folder.
