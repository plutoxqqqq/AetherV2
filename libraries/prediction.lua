local genv=getgenv and getgenv() or _G;if not _G.vape or not genv or not genv.used_init then return {} end;local module={};local eps=1e-9;local cloneref=cloneref or function(ref)return ref end;local playersService=cloneref(game:GetService('Players'));local tweenService=cloneref(game:GetService('TweenService'));local statsService=cloneref(game:GetService('Stats'));local responseTime;local function sampleRtt()local ok,ping=pcall(function()return statsService.Network.ServerStatsItem['Data Ping']:GetValue()end);if ok and type(ping)=='number'and ping>0 then return ping/1000 end;return 0 end;function module.setLatency(value)if value==nil then responseTime=nil elseif type(value)=='number'and value==value and value>0 and value<math.huge then responseTime=value end end;local latencyBias=0;local function getBaseLatency()if store and store.ping and store.ping.total then return store.ping.total end;if responseTime then return responseTime end;local ok,ping=pcall(playersService.LocalPlayer.GetNetworkPing,playersService.LocalPlayer);if ok and type(ping)=='number'and ping>=0 then return ping*2 end;return sampleRtt()end;function module.getLatency()local latency=getBaseLatency();if type(latency)~='number' or latency~=latency or latency<0 or latency==math.huge then latency=0 end;return math.max(latency+latencyBias,0)end;function module.getRawLatency()return getBaseLatency()end;function module.Raycast(origin,direction,params)return workspace:Raycast(origin,direction,params)end;module.IsTrajectoryClear=function(origin,velocity,gravity,travelTime,params,target,ignored)if typeof(origin)~='Vector3'or typeof(velocity)~='Vector3'or type(gravity)~='number'or type(travelTime)~='number'or travelTime<0 or not params or(target and typeof(target)~='Instance')or(ignored and type(ignored)~='function')then return false end;local rayParams=RaycastParams.new();rayParams.FilterType=params.FilterType;rayParams.FilterDescendantsInstances=table.clone(params.FilterDescendantsInstances or {});rayParams.IgnoreWater=params.IgnoreWater;rayParams.CollisionGroup=params.CollisionGroup;rayParams.RespectCanCollide=params.RespectCanCollide;local lastPosition=origin;for i=1,10 do local time=travelTime*(i/10);local position=origin+velocity*time+Vector3.new(0,-gravity*0.5*time*time,0);local direction=position-lastPosition;local hit=module.Raycast(lastPosition,direction,rayParams);local ignoredHits=0;while hit do ignoredHits+=1;if ignoredHits>64 then return false,hit end;if target and hit.Instance:IsDescendantOf(target)then return true end;if not ignored or not ignored(hit.Instance)then return false,hit end;rayParams:AddToFilter(hit.Instance);hit=module.Raycast(lastPosition,direction,rayParams)end;lastPosition=position end;return true end;local tracer=Instance.new('Part');tracer.Anchored=true;tracer.CanCollide=false;tracer.CanQuery=false;tracer.CanTouch=false;tracer.CastShadow=false;local isZero=function(d)return(d>-eps and d<eps)end;local cuberoot=function(x)return(x>0)and math.pow(x,(1/3))or -math.pow(math.abs(x),(1/3))end;local solveQuadric=function(c0,c1,c2)local s0,s1;if isZero(c0)then return not isZero(c1)and -c2/c1 or nil end;local p,q,D;p=c1/(2*c0);q=c2/c0;D=p*p-q;if isZero(D)then s0=-p;return s0 elseif(D<0)then return else local sqrt_D=math.sqrt(D);s0=sqrt_D-p;s1=-sqrt_D-p;return s0,s1 end end;local solveCubic=function(c0,c1,c2,c3)local s0,s1,s2;if isZero(c0)then return solveQuadric(c1,c2,c3)end;local num,sub;local A,B,C;local sq_A,p,q;local cb_p,D;A=c1/c0;B=c2/c0;C=c3/c0;sq_A=A*A;p=(1/3)*(-(1/3)*sq_A+B);q=0.5*((2/27)*A*sq_A-(1/3)*A*B+C);cb_p=p*p*p;D=q*q+cb_p;if isZero(D)then if isZero(q)then s0=0;num=1 else local u=cuberoot(-q);s0=2*u;s1=-u;num=2 end elseif(D<0)then local phi=(1/3)*math.acos(math.clamp(-q/math.sqrt(-cb_p),-1,1));local t=2*math.sqrt(-p);s0=t*math.cos(phi);s1=-t*math.cos(phi+math.pi/3);s2=-t*math.cos(phi-math.pi/3);num=3 else local sqrt_D=math.sqrt(D);local u=cuberoot(sqrt_D-q);local v=-cuberoot(sqrt_D+q);s0=u+v;num=1 end;sub=(1/3)*A;if(num>0)then s0=s0-sub end;if(num>1)then s1=s1-sub end;if(num>2)then s2=s2-sub end;return s0,s1,s2 end;function module.solveQuartic(c0,c1,c2,c3,c4)local s0,s1,s2,s3;if isZero(c0)then local root0,root1,root2=solveCubic(c1,c2,c3,c4);local cubic={};if root0 then table.insert(cubic,root0)end;if root1 then table.insert(cubic,root1)end;if root2 then table.insert(cubic,root2)end;return cubic end;local coeffs={};local z,u,v,sub;local A,B,C,D;local sq_A,p,q,r;local num;A=c1/c0;B=c2/c0;C=c3/c0;D=c4/c0;sq_A=A*A;p=-0.375*sq_A+B;q=0.125*sq_A*A-0.5*A*B+C;r=-(3/256)*sq_A*sq_A+0.0625*sq_A*B-0.25*A*C+D;if isZero(r)then coeffs[3]=q;coeffs[2]=p;coeffs[1]=0;coeffs[0]=1;s0,s1,s2=solveCubic(coeffs[0],coeffs[1],coeffs[2],coeffs[3]);num=(s0 and 1 or 0)+(s1 and 1 or 0)+(s2 and 1 or 0)else coeffs[3]=0.5*r*p-0.125*q*q;coeffs[2]=-r;coeffs[1]=-0.5*p;coeffs[0]=1;s0,s1,s2=solveCubic(coeffs[0],coeffs[1],coeffs[2],coeffs[3]);z=s0;u=z*z-r;v=2*z-p;if isZero(u)then u=0 elseif(u>0)then u=math.sqrt(u)else return end;if isZero(v)then v=0 elseif(v>0)then v=math.sqrt(v)else return end;coeffs[2]=z-u;coeffs[1]=q<0 and -v or v;coeffs[0]=1;s0,s1=solveQuadric(coeffs[0],coeffs[1],coeffs[2]);num=(s0 and 1 or 0)+(s1 and 1 or 0);coeffs[2]=z+u;coeffs[1]=q<0 and v or -v;coeffs[0]=1;if(num==0)then local root0,root1=solveQuadric(coeffs[0],coeffs[1],coeffs[2]);num=num+(root0 and 1 or 0)+(root1 and 1 or 0);s0,s1=root0,root1 end;if(num==1)then local root0,root1=solveQuadric(coeffs[0],coeffs[1],coeffs[2]);num=num+(root0 and 1 or 0)+(root1 and 1 or 0);s1,s2=root0,root1 end;if(num==2)then local root0,root1=solveQuadric(coeffs[0],coeffs[1],coeffs[2]);num=num+(root0 and 1 or 0)+(root1 and 1 or 0);s2,s3=root0,root1 end end;sub=0.25*A;local roots={};if(num>3)then table.insert(roots,s3-sub)end;if(num>2)then table.insert(roots,s2-sub)end;if(num>1)then table.insert(roots,s1-sub)end;if(num>0)then table.insert(roots,s0-sub)end;return roots end;module.SpawnTracer=function(from,to,custom)if typeof(from)~='Vector3'or typeof(to)~='Vector3'or type(custom)~='table'then return end;local distance=(to-from).Magnitude;if distance<0.01 then return end;local t=tracer:Clone();t.Color=custom.Color;t.Size=Vector3.new(custom.Thick,custom.Thick,distance);t.CFrame=CFrame.lookAt(from,to)*CFrame.new(0,0,-distance/2);t.Material=custom.Material;t.Transparency=custom.Opacity or 0;if custom.Fade then tweenService:Create(t,TweenInfo.new(custom.Lifetime),{Transparency=1}):Play()end;return t end;module.SpawnArcTracer=function(origin,aimDirection,projectileSpeed,gravity,travelTime,steps,custom)if typeof(origin)~='Vector3'or typeof(aimDirection)~='Vector3'or type(projectileSpeed)~='number'or type(gravity)~='number'or type(travelTime)~='number'or travelTime<0 or type(custom)~='table'then return end;steps=steps or 20;if type(steps)~='number'or steps<1 then return end;local stepTime=travelTime/steps;local g=Vector3.new(0,-gravity,0);local velocity=aimDirection*projectileSpeed;local prevPos=origin;local model=Instance.new('Model');model.Parent=workspace.Terrain;if custom.Material==Enum.Material.Glass then Instance.new('Highlight',model).Enabled=false end;for i=1,steps do local t=i*stepTime;local nextPos=origin+velocity*t+0.5*g*t*t;local l=module.SpawnTracer(prevPos,nextPos,custom);if l then l.Parent=model end;prevPos=nextPos end;task.delay(custom.Lifetime,model.Destroy,model)end;local validNumber=function(value)return type(value)=='number'and value==value and math.abs(value)<math.huge end;local horizontalMask=Vector3.new(1,0,1);local targetMotion=setmetatable({},{__mode='k'});local validVector=function(value)return typeof(value)=='Vector3'and validNumber(value.X)and validNumber(value.Y)and validNumber(value.Z)end;local function getAttribute(root,attribute)local character=root and root.Parent;if character and character:IsA('Model')then return character:GetAttribute(attribute)end end;local function resetMotion(root,state,position,velocity,time,spawnTime)local knockbackMark=state.knockbackMark;local knockbackMultiplier=state.knockbackMultiplier;local jumpVelocity=state.jumpVelocity;local jumpPeriod=state.jumpPeriod;table.clear(state);state.jumpVelocity=jumpVelocity;state.jumpPeriod=jumpPeriod;state.assembly=velocity;state.damageTime=getAttribute(root,'LastDamageTakenTime');state.knockbackMark=knockbackMark;state.knockbackMultiplier=knockbackMultiplier;state.position=position;state.samples={{position=position,time=time}};state.spawnTime=spawnTime;state.stableVelocity=velocity;state.time=time;state.velocity=velocity;targetMotion[root]=state;return state end;function module.markKnockback(target,multiplier,impulse)if typeof(target)~='Instance'then return end;if type(multiplier)~='number'or multiplier~=multiplier or multiplier==math.huge or multiplier==-math.huge then multiplier=1 end;local root=target:IsA('BasePart')and target or target:IsA('Model')and target.PrimaryPart;if not root then return end;local state=targetMotion[root]or{};state.knockbackMark=workspace:GetServerTimeNow();state.knockbackMultiplier=multiplier;if validVector(impulse)then local horizontal=impulse*horizontalMask;state.knockbackHint=horizontal.Magnitude>eps and horizontal or nil;state.knockbackLift=math.abs(impulse.Y)>eps and impulse.Y or nil;state.knockbackHintTime=state.knockbackMark end;state.expectedImpulse=nil;state.expectedLift=nil;state.expectedTime=nil;targetMotion[root]=state end;function module.expectKnockback(target,arrival,impulse,multiplier)if typeof(target)~='Instance'then return end;if type(multiplier)~='number'or multiplier~=multiplier or multiplier==math.huge or multiplier==-math.huge then multiplier=1 end;local root=target:IsA('BasePart')and target or target:IsA('Model')and target.PrimaryPart;if not root or not validNumber(arrival)or not validVector(impulse)then return end;local horizontal=impulse*horizontalMask;if horizontal.Magnitude<=eps and math.abs(impulse.Y)<=eps then return end;local state=targetMotion[root]or{};state.expectedImpulse=horizontal;state.expectedLift=impulse.Y;state.expectedTime=arrival;state.expectedMultiplier=multiplier;targetMotion[root]=state end;local pendingShots={};local residualSpread,residualSamples=0,0;local epochHits,epochShots,epochRate=0,0,nil;local stepDirection,stepSize=1,0.008;local function applyEpoch()if epochShots<12 then return end;local rate=epochHits/epochShots;epochHits,epochShots=0,0;if epochRate and rate+0.02<epochRate then stepDirection=-stepDirection;stepSize=math.max(stepSize*0.7,0.002)end;epochRate=rate;if rate>=0.95 then return end;latencyBias=math.clamp(latencyBias+stepDirection*stepSize,-0.12,0.12)end;local function measureSpread(shot)local root=shot.root;if typeof(root)~='Instance'or not root.Parent then return end;local state=targetMotion[root];if not state or state.spawnTime~=shot.spawnTime then return end;if state.knockbackUntil and state.knockbackUntil>shot.fireTime then return end;local residual=(root.Position-shot.impact)*horizontalMask;if not validVector(residual)or residual.Magnitude>30 then return end;local cross=(residual-shot.direction*residual:Dot(shot.direction)).Magnitude;residualSpread=residualSpread+(cross-residualSpread)*0.2;residualSamples=math.min(residualSamples+1,400)end;local function resolveShots()if #pendingShots==0 then return end;local now=workspace:GetServerTimeNow();for index=#pendingShots,1,-1 do local shot=pendingShots[index];if not shot.measured and now>=shot.dueTime then shot.measured=true;measureSpread(shot)end;if now>shot.dueTime+0.35 then table.remove(pendingShots,index);epochShots+=1;applyEpoch()end end end;function module.trackShot(targetRoot)if typeof(targetRoot)~='Instance'then return end;local state=targetMotion[targetRoot];local solution=state and state.lastSolution;if not solution or not validNumber(solution.time)or not validNumber(solution.travelTime)or not validVector(solution.impact)then return end;local velocity=(state.velocity or Vector3.zero)*horizontalMask;if velocity.Magnitude<8 then return end;if #pendingShots>=24 then table.remove(pendingShots,1)end;table.insert(pendingShots,{direction=velocity.Unit,dueTime=solution.time+solution.travelTime,fireTime=solution.time,impact=solution.impact,root=targetRoot,spawnTime=state.spawnTime,speed=velocity.Magnitude})end;function module.reportHit(targetRoot)if typeof(targetRoot)~='Instance'then return end;local now=workspace:GetServerTimeNow();for index=#pendingShots,1,-1 do local shot=pendingShots[index];if shot.root==targetRoot and now>=shot.dueTime-0.35 and now<=shot.dueTime+0.35 then if not shot.measured then shot.measured=true;measureSpread(shot)end;table.remove(pendingShots,index);epochHits+=1;epochShots+=1;applyEpoch();return end end end;function module.getResidualSpread()return residualSpread,residualSamples end;function module.getLatencyBias()return latencyBias,epochRate end;local worldFilter;local learnedParams=RaycastParams.new();learnedParams.FilterType=Enum.RaycastFilterType.Include;local excludeParams=RaycastParams.new();excludeParams.FilterType=Enum.RaycastFilterType.Exclude;excludeParams.IgnoreWater=true;excludeParams.RespectCanCollide=true;local excludeList,excludeTime={},0;local castFloor=function(position,params)return module.Raycast(position,Vector3.new(0,workspace.FallenPartsDestroyHeight-position.Y,0),params)end;local function refreshExcludeList(root)local time=os.clock();if time-excludeTime>1 then excludeTime=time;table.clear(excludeList);for _,player in playersService:GetPlayers()do if player.Character then table.insert(excludeList,player.Character)end end end;local character=root and root.Parent;if character and not table.find(excludeList,character)then table.insert(excludeList,character)end;return excludeList end;local traceFloor=function(position,params,root)if params then local ray=castFloor(position,params);if ray then if params.FilterType==Enum.RaycastFilterType.Include then worldFilter=params.FilterDescendantsInstances end;return ray.Position.Y end end;if worldFilter and #worldFilter>0 then learnedParams.FilterDescendantsInstances=worldFilter;local ray=castFloor(position,learnedParams);if ray then return ray.Position.Y end end;excludeParams.FilterDescendantsInstances=refreshExcludeList(root);local ray=castFloor(position,excludeParams);return ray and ray.Position.Y or nil end;local getTargetMotion=function(root,position,velocity,airborne,playerGravity)if not root or not validVector(position)or not validVector(velocity)then return{acceleration=Vector3.zero,position=position,velocity=velocity}end;local time=workspace:GetServerTimeNow();local spawnTime=getAttribute(root,'SpawnTime');local state=targetMotion[root]or{};if state.time and time-state.time<1/240 then local knockback=state.knockbackUntil and time<state.knockbackUntil;return{acceleration=Vector3.zero,decay=state.knockbackDecay,flying=state.flying,impulse=knockback and state.knockbackImpulse,knockback=knockback,knockbackBase=knockback and state.knockbackBase,expectedImpulse=state.expectedImpulse,expectedLift=state.expectedLift,expectedTime=state.expectedTime,missRate=state.missRate,position=state.position,state=state,stale=state.updateInterval and math.clamp(state.updateInterval*0.5,0,0.06)or nil,strafeElapsed=state.strafeReversal and time-state.strafeReversal or nil,strafeHalf=state.strafeHalf,strafeSeen=state.strafeSeen,strafeSpeed=state.strafeSpeed,strafeDirection=state.strafeDirection,turnRate=state.turnRate,turnSeen=state.turnSeen,velocity=state.velocity}end;if not state.time or time-state.time>0.75 or state.spawnTime~=spawnTime then state=resetMotion(root,state,position,velocity,time,spawnTime);return{acceleration=Vector3.zero,position=position,state=state,velocity=velocity}end;local delta=time-state.time;if delta<=eps then return{acceleration=Vector3.zero,flying=state.flying,position=state.position,state=state,velocity=state.velocity}end;local displacement=position-state.position;local instantVelocity=displacement/delta;local assemblyAgreement=velocity.Magnitude>5 and instantVelocity.Magnitude>5 and velocity.Unit:Dot(instantVelocity.Unit)>0.75 and math.abs(velocity.Magnitude-instantVelocity.Magnitude)<math.max(40,velocity.Magnitude*0.75);if state.pendingPosition then if(position-state.position).Magnitude<2 then state.pendingPosition=nil;state.pendingTime=nil elseif(position-state.pendingPosition).Magnitude<2 then state=resetMotion(root,state,position,velocity,time,spawnTime);return{acceleration=Vector3.zero,changed=true,position=position,state=state,velocity=velocity}else state.pendingPosition=nil;state.pendingTime=nil end end;local displacementLimit=math.max(12,(math.max(state.velocity.Magnitude,velocity.Magnitude)+30)*delta*1.5);if(displacement.Magnitude>displacementLimit or instantVelocity.Magnitude>260)and not assemblyAgreement then state.pendingPosition=position;state.pendingTime=time;state.time=time;return{acceleration=Vector3.zero,flying=state.flying,invalid=true,position=state.position,state=state,velocity=state.velocity}end;local samples=state.samples;table.insert(samples,{position=position,time=time});while #samples>6 or(#samples>2 and time-samples[1].time>0.14)do table.remove(samples,1)end;local measuredVelocity;local oldest=samples[1];if oldest and time-oldest.time>0.02 then measuredVelocity=(position-oldest.position)/(time-oldest.time);if not validVector(measuredVelocity)or measuredVelocity.Magnitude>260 then measuredVelocity=nil end end;local recentVelocity;local previousSample=samples[#samples-1];if previousSample and time-previousSample.time>eps then recentVelocity=(position-previousSample.position)/(time-previousSample.time);if not validVector(recentVelocity)or recentVelocity.Magnitude>260 then recentVelocity=nil end end;local damageTime=getAttribute(root,'LastDamageTakenTime');local damageChanged=validNumber(damageTime)and damageTime>0 and damageTime~=state.damageTime;local marked=time-(state.knockbackMark or -math.huge)<0.25;local velocityChange=velocity-state.assembly;local measuredAgreement=measuredVelocity and measuredVelocity.Magnitude>5 and velocity.Magnitude>5 and measuredVelocity.Unit:Dot(velocity.Unit)>0.7 and(measuredVelocity-velocity).Magnitude<math.max(35,velocity.Magnitude*0.6);local velocityInvalid=(velocityChange.Magnitude>math.max(70,state.velocity.Magnitude*2+35)or velocity.Magnitude>260)and not marked and not damageChanged and not measuredAgreement;if velocityInvalid then local candidate=state.velocityCandidate;if candidate and time-state.velocityCandidateTime<0.12 and(candidate-velocity).Magnitude<math.max(12,velocity.Magnitude*0.2)then state.velocityCandidate=nil;state.velocityCandidateTime=nil else state.velocityCandidate=velocity;state.velocityCandidateTime=time;velocity=state.assembly end else state.velocityCandidate=nil;state.velocityCandidateTime=nil end;local assemblyHorizontal=velocity*horizontalMask;local measuredHorizontal=measuredVelocity and measuredVelocity*horizontalMask;local horizontalVelocity=assemblyHorizontal;local measuredAccepted;if measuredHorizontal then local difference=(measuredHorizontal-assemblyHorizontal).Magnitude;if difference<math.max(6,assemblyHorizontal.Magnitude*0.35)then horizontalVelocity=assemblyHorizontal:Lerp(measuredHorizontal,0.15);measuredAccepted=true elseif assemblyHorizontal.Magnitude<2 and measuredHorizontal.Magnitude>2 then local candidate=state.measuredCandidate;local consistent=candidate and time-state.measuredCandidateTime<0.15 and candidate.Magnitude>eps and measuredHorizontal.Magnitude>eps and candidate.Unit:Dot(measuredHorizontal.Unit)>0.8 and math.abs(candidate.Magnitude-measuredHorizontal.Magnitude)<math.max(12,measuredHorizontal.Magnitude*0.35);if measuredHorizontal.Magnitude<45 or consistent then horizontalVelocity=measuredHorizontal;measuredAccepted=true;state.measuredCandidate=nil;state.measuredCandidateTime=nil else state.measuredCandidate=measuredHorizontal;state.measuredCandidateTime=time;horizontalVelocity=state.velocity*horizontalMask end else state.measuredCandidate=nil;state.measuredCandidateTime=nil end end;local verticalVelocity=velocity.Y;if measuredAccepted and measuredVelocity then if math.abs(verticalVelocity-measuredVelocity.Y)<8 then verticalVelocity=verticalVelocity+(measuredVelocity.Y-verticalVelocity)*0.15 elseif recentVelocity and math.abs(verticalVelocity)<1 and(state.flying or math.abs(recentVelocity.Y)<45)then verticalVelocity=recentVelocity.Y end end;if not airborne and math.abs(verticalVelocity)<2 then verticalVelocity=0 end;local combinedVelocity=Vector3.new(horizontalVelocity.X,verticalVelocity,horizontalVelocity.Z);local previousVelocity=state.velocity;local difference=(combinedVelocity-previousVelocity).Magnitude;local previousHorizontal=previousVelocity*horizontalMask;local directionChanged=horizontalVelocity.Magnitude>2 and previousHorizontal.Magnitude>2 and horizontalVelocity.Unit:Dot(previousHorizontal.Unit)<0.65;local shift=difference/math.max(previousVelocity.Magnitude*0.35,7);local changed=shift>=1 or directionChanged;local currentVelocity;if changed then currentVelocity=combinedVelocity else local alpha=math.clamp(delta*20,0.2,1);currentVelocity=previousVelocity:Lerp(combinedVelocity,alpha+(1-alpha)*shift*shift)end;local humanoid=root.Parent and root.Parent:FindFirstChildWhichIsA('Humanoid');local impulseChange=(currentVelocity-previousVelocity)*horizontalMask;local knockbackStarted=(marked or damageChanged)and(impulseChange.Magnitude>1.5 or math.abs(currentVelocity.Y-previousVelocity.Y)>1.5);if not knockbackStarted and humanoid and humanoid.PlatformStand then knockbackStarted=impulseChange.Magnitude>5 or math.abs(currentVelocity.Y-previousVelocity.Y)>5 end;if state.expectedTime then if knockbackStarted or time>state.expectedTime+0.35 then state.expectedImpulse,state.expectedTime,state.expectedMultiplier,state.expectedLift=nil,nil,nil,nil elseif time>=state.expectedTime then state.knockbackHint=state.expectedImpulse;state.knockbackLift=state.expectedLift;state.knockbackHintTime=time;state.knockbackMark=time;state.knockbackMultiplier=state.expectedMultiplier;state.expectedImpulse,state.expectedTime,state.expectedMultiplier,state.expectedLift=nil,nil,nil,nil end end;local hint=state.knockbackHint;if hint and validVector(hint) and state.knockbackHintTime and time-state.knockbackHintTime<0.25 then local lift=state.knockbackLift or 0;local impulse=Vector3.new(hint.X,lift,hint.Z);local multiplier=state.knockbackMultiplier or 1;state.knockbackImpulse=impulse*multiplier;state.knockbackBase=previousVelocity;state.knockbackUntil=time+0.35;state.knockbackDecay=math.max(0.01,1-math.clamp(delta*8,0,0.95));state.knockbackHint=nil;state.knockbackHintTime=nil end;state.position=position;state.time=time;state.assembly=velocity;state.velocity=currentVelocity;state.stableVelocity=state.stableVelocity and state.stableVelocity:Lerp(currentVelocity,math.clamp(delta*8,0,1))or currentVelocity;local acceleration=(currentVelocity-previousVelocity)/math.max(delta,eps);state.acceleration=acceleration;state.flying=airborne;state.updateInterval=delta;state.damageTime=damageTime;local knockback=state.knockbackUntil and time<state.knockbackUntil;return{acceleration=acceleration,decay=state.knockbackDecay,flying=state.flying,impulse=knockback and state.knockbackImpulse,knockback=knockback,knockbackBase=knockback and state.knockbackBase,expectedImpulse=state.expectedImpulse,expectedLift=state.expectedLift,expectedTime=state.expectedTime,missRate=state.missRate,position=position,state=state,stale=nil,strafeElapsed=state.strafeReversal and time-state.strafeReversal or nil,strafeHalf=state.strafeHalf,strafeSeen=state.strafeSeen,strafeSpeed=state.strafeSpeed,strafeDirection=state.strafeDirection,turnRate=state.turnRate,turnSeen=state.turnSeen,velocity=currentVelocity}end;module.getTargetMotion=getTargetMotion;module.resolveShots=resolveShots;return module